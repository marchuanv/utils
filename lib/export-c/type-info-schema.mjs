import {
    TypeInfo
} from "../../registry.mjs";
export class TypeInfoSchema extends TypeInfo {
    /**
     * @param { Object } data
    */
    validate(data) {
        super.validate(data);
    }
}